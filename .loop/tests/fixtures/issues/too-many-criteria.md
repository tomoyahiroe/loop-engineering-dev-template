## 背景
背景の説明。

## 受け入れ基準
- [ ] `packages/web で pnpm test -- 1` が緑
- [ ] `packages/web で pnpm test -- 2` が緑
- [ ] `packages/web で pnpm test -- 3` が緑
- [ ] `packages/web で pnpm test -- 4` が緑
- [ ] `packages/web で pnpm test -- 5` が緑
- [ ] `packages/web で pnpm test -- 6` が緑
- [ ] `packages/web で pnpm test -- 7` が緑

## 実装方針
`packages/web/src/a.ts` を変更する。

## スコープ外
なし

## 依存
なし
